# BTC Hedge Assistant learning data

This directory is reserved for anonymized strategy-performance datasets and aggregate reports. Do not commit API keys, account balances, wallet addresses, emails, or other private account data.

Browser-side collection is implemented in `learning.js`. It records only signal/market metadata locally and can export a JSON dataset for later analysis.
