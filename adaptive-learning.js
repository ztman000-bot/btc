/* BTC Hedge Assistant v8.15.0 - Robust Adaptive Optimizer */
(()=>{'use strict';
const V='8.15.0',K='btc_adaptive_learning_v8150',$=id=>document.getElementById(id),clamp=(x,a,b)=>Math.max(a,Math.min(b,x));
function M(){try{return typeof market!=='undefined'?market:(window.market||{})}catch(e){return window.market||{}}}
function P(){try{return typeof pos==='function'?pos():(window.pos?.())}catch(e){return null}}
function rows(tf){const a=M()?.[tf]||[];return Array.isArray(a)?a:[]}
function closeRows(){return rows('h4').map(r=>({t:Number(Array.isArray(r)?r[0]:(r.time??r.t??0)),c:Number(Array.isArray(r)?r[4]:(r.close??r.c)),h:Number(Array.isArray(r)?r[2]:(r.high??r.h)),l:Number(Array.isArray(r)?r[3]:(r.low??r.l))})).filter(x=>Number.isFinite(x.c))}
function closes(){return closeRows().map(x=>x.c)}
function ema(a,p){if(!a.length)return[];let e=a[0],k=2/(p+1);return a.map(v=>(e=v*k+e*(1-k)))}
function atrPct(){const a=closeRows();if(a.length<20)return .03;let tr=[];for(let i=1;i<a.length;i++){const hi=a[i].h,lo=a[i].l,pc=a[i-1].c;if([hi,lo,pc].every(Number.isFinite))tr.push(Math.max(hi-lo,Math.abs(hi-pc),Math.abs(lo-pc)))}const x=tr.slice(-14),p=a.at(-1)?.c||1;return x.length?x.reduce((s,v)=>s+v,0)/x.length/p:.03}
function regimeScore(){return window.BTCRegimeHedge?.trendScore?.()?.score??50}
function load(){try{return JSON.parse(localStorage.getItem(K)||'null')||{champion:{fast:20,slow:50,dead:.0015,confirm:2,name:'BASE'},candidate:null,streak:0,demoteStreak:0,cycles:0,evidence:0}}catch(e){return{champion:{fast:20,slow:50,dead:.0015,confirm:2,name:'BASE'},candidate:null,streak:0,demoteStreak:0,cycles:0,evidence:0}}}
function save(x){try{localStorage.setItem(K,JSON.stringify(x))}catch(e){}}
function signature(){const a=closeRows(),x=a.at(-1);return x?`${x.t||a.length}:${x.c}`:`none:${a.length}`}
function candidates(){const out=[{fast:20,slow:50,dead:.0015,confirm:2,name:'BASE'}],F=[8,12,16,20,24,30],S=[36,48,60,72,96,120],D=[0,.0015,.003],C=[1,2,3];for(const f of F)for(const s of S)if(s>=f*1.8)for(const d of D)for(const c of C){if(f===20&&s===50&&d===.0015&&c===2)continue;out.push({fast:f,slow:s,dead:d,confirm:c,name:`E${f}/${s} d${(d*100).toFixed(2)} c${c}`})}return out}
function runFold(all,start,end,p,fee=.0005){const f=ema(all,p.fast),s=ema(all,p.slow);let sig=0,pending=0,count=0,eq=1,pk=1,mdd=0,turn=0;for(let i=1;i<end;i++){const up=f[i]>s[i]*(1+p.dead),dn=f[i]<s[i]*(1-p.dead),raw=up?1:dn?-1:sig;if(raw!==sig){if(raw===pending)count++;else{pending=raw;count=1}if(count>=p.confirm){if(i>=start)turn+=Math.abs(raw-sig);sig=raw;pending=0;count=0}}else{pending=0;count=0}if(i>=start){const r=all[i]/all[i-1]-1;eq*=Math.max(.05,1+sig*r-Math.abs(turn?0:0)*0);if(i===start&&sig!==0)eq*=1-fee*.25;pk=Math.max(pk,eq);mdd=Math.max(mdd,(pk-eq)/pk)}}eq*=Math.max(.05,1-turn*fee);return{ret:eq-1,mdd,turn}}
function robustSearch(){const a=closes();if(a.length<420)return{ok:false,note:`4H 표본 ${a.length}개 · 최소 420개 필요`,tested:0};const test=48,train=240,maxFolds=6,folds=[];for(let end=a.length;end>=train+test&&folds.length<maxFolds;end-=test)folds.push({start:end-test,end,base:end-train-test});if(folds.length<4)return{ok:false,note:'독립 OOS 구간 부족',tested:0};const list=candidates(),stats=[];for(const p of list){let rr=[];for(const f of folds){const slice=a.slice(f.base,f.end),start=slice.length-test;rr.push(runFold(slice,start,slice.length,p))}const rets=rr.map(x=>x.ret).sort((x,y)=>x-y),dds=rr.map(x=>x.mdd),turns=rr.map(x=>x.turn),avg=rets.reduce((s,x)=>s+x,0)/rets.length,med=rets[Math.floor(rets.length/2)],worst=rets[0],mdd=dds.reduce((s,x)=>s+x,0)/dds.length,turn=turns.reduce((s,x)=>s+x,0)/turns.length,score=med+.35*avg+.25*worst-.9*mdd-.0008*turn;stats.push({...p,score,avg,med,worst,mdd,turn,folds:rr.length})}stats.sort((a,b)=>b.score-a.score);const best=stats[0],base=stats.find(x=>x.name==='BASE')||best;const promote=best.name!=='BASE'&&best.score>base.score+.004&&best.med>=base.med&&best.worst>=base.worst-.01&&best.mdd<=base.mdd*1.10;return{ok:true,best,base,promote,top:stats.slice(0,5),tested:list.length,folds:folds.length}}
function currentSignal(p){const a=closes();if(a.length<Math.max(p.slow,60))return{dir:0,strength:0};const f=ema(a,p.fast),s=ema(a,p.slow),fv=f.at(-1),sv=s.at(-1),vol=Math.max(.005,atrPct()),gap=(fv-sv)/sv,dir=gap>p.dead?1:gap<-p.dead?-1:0,strength=clamp(Math.abs(gap)/(vol*1.6),0,1);return{dir,strength,gap}}
function recoveryProgress(){try{const d=P(),p=Number(M()?.price)||closes().at(-1);if(!d||!p||!window.BTCRecoveryEngine?.closeWallet)return 0;const now=window.BTCRecoveryEngine.closeWallet(d,p),target=(d.wallet||69936);return clamp(now/Math.max(1,target),0,1)}catch(e){return 0}}
function continuous(param){const rs=regimeScore(),sg=currentSignal(param),vol=atrPct(),progress=recoveryProgress(),reg=(rs-50)/50,blend=clamp(.6*reg+.4*sg.dir*sg.strength,-1,1),conf=Math.abs(blend),riskScale=clamp(1-.40*progress,.58,1);let longKeep=1,shortKeep=1;if(blend>0){shortKeep=clamp(1-(.40*conf*riskScale),.58,.98);longKeep=clamp(1-(.16*conf*(1-riskScale*.25)),.78,1)}else if(blend<0){longKeep=clamp(1-(.44*conf*riskScale),.52,.98);shortKeep=clamp(1-(.14*conf*(1-riskScale*.25)),.80,1)}return{longKeep,shortKeep,conf,vol,blend,progress}}
function guard(c){const p=closes().at(-1)||0,atr=p*c.vol;const base=Math.max(10000,2.6*atr+2000),lockBoost=c.progress>.75?3000:c.progress>.5?1500:0;return Math.round(Math.min(26000,base+lockBoost)/500)*500}
function learn(){const st=load(),wf=robustSearch(),sig=signature();st.cycles=(st.cycles||0)+1;const newEvidence=sig!==st.lastEvidence;if(newEvidence){st.lastEvidence=sig;st.evidence=(st.evidence||0)+1;if(wf.ok){const proposed=wf.promote?wf.best:{fast:20,slow:50,dead:.0015,confirm:2,name:'BASE'};const key=`${proposed.fast}/${proposed.slow}/${proposed.dead}/${proposed.confirm}`;if(st.candidateKey===key)st.streak=(st.streak||0)+1;else{st.candidateKey=key;st.candidate=proposed;st.streak=1}const champ=st.champion||{fast:20,slow:50,dead:.0015,confirm:2,name:'BASE'};const champRow=wf.top?.find(x=>x.fast===champ.fast&&x.slow===champ.slow&&x.dead===champ.dead&&x.confirm===champ.confirm);if(wf.promote&&st.streak>=3)st.champion=proposed;if(champ.name!=='BASE'&&champRow&&champRow.score<wf.base.score-.006){st.demoteStreak=(st.demoteStreak||0)+1;if(st.demoteStreak>=2){st.champion={fast:20,slow:50,dead:.0015,confirm:2,name:'BASE'};st.demoteStreak=0}}else st.demoteStreak=0}}}st.at=Date.now();save(st);const champion=st.champion||{fast:20,slow:50,dead:.0015,confirm:2,name:'BASE'},c=continuous(champion),g=guard(c);return{st,wf,champion,c,g,newEvidence}}
function pane(){return $('v850HomePane')||document.querySelector('main>.v853Active')||document.querySelector('main')}
function ensure(){let c=$('adaptiveLearningCard');if(!c){c=document.createElement('div');c.id='adaptiveLearningCard';c.className='card'}const p=pane();if(p&&c.parentElement!==p)p.appendChild(c);c.classList.remove('v853Page','v852Page','v851Page','tabPane');c.style.cssText+=';display:block;width:100%;max-width:100%;min-width:0';return c}
function moneyPct(x){return(x*100).toFixed(2)+'%'}
function render(){const c=ensure(),d=learn();if(!c)return;const wf=d.wf.ok?`검색 ${d.wf.tested}개 · OOS ${d.wf.folds}구간 · 1위 ${d.wf.best.name} · ${d.wf.promote?'승격 검증 중':'BASE 대비 충분한 우위 없음'}`:d.wf.note;const champ=d.champion;c.innerHTML=`<div class="row"><div><b>🧠 Robust Adaptive Optimizer</b><div class="small">강건 최적화 · 새 4H 증거에서만 학습 진전</div></div><span class="badge">v${V}</span></div><div class="grid3"><div class="kpi"><div class="small">현재 Champion</div><b>${champ.name}</b><div class="tiny">EMA ${champ.fast}/${champ.slow} · dead ${(champ.dead*100).toFixed(2)}% · 확인 ${champ.confirm}</div></div><div class="kpi"><div class="small">학습 증거</div><b>${d.st.evidence||0}회</b><div class="tiny">계산 ${d.st.cycles||0}회 · 같은 4H바 반복은 증거로 미인정</div></div><div class="kpi"><div class="small">승격 연속증거</div><b>${d.st.streak||0}/3</b><div class="tiny">새 4H 데이터 3회 필요</div></div></div><div class="grid3" style="margin-top:8px"><div class="kpi"><div class="small">권장 롱 유지</div><b>${moneyPct(d.c.longKeep)}</b></div><div class="kpi"><div class="small">권장 숏 유지</div><b>${moneyPct(d.c.shortKeep)}</b></div><div class="kpi"><div class="small">동적 안전거리</div><b>$${Math.round(d.g/1000)}K</b><div class="tiny">절대 하한 $10K</div></div></div><div class="${d.wf.promote?'okbox':'warnbox'}"><b>${wf}</b><br>${d.wf.ok?`1위 점수 ${d.wf.best.score.toFixed(4)} · BASE ${d.wf.base.score.toFixed(4)} · 1위 MDD ${moneyPct(d.wf.best.mdd)} · 최악 OOS ${moneyPct(d.wf.best.worst)}`:''}</div><div class="small">Recovery 진행률 ${(d.c.progress*100).toFixed(1)}% · 회복률이 높아질수록 자동으로 공격성을 낮춥니다. 최적화 목적함수는 중앙 OOS 성과 + 평균성과 + 최악구간 − MDD − 회전율입니다. 자동주문은 하지 않습니다.</div>`}
function boot(){render();[800,2500,7000].forEach(x=>setTimeout(render,x));setInterval(render,45000)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();window.BTCAdaptiveLearning={version:V,learn,robustSearch,continuous,render,recommendation:()=>{const d=learn();return{champion:d.champion,longKeep:d.c.longKeep,shortKeep:d.c.shortKeep,guard:d.g,progress:d.c.progress}}};})();