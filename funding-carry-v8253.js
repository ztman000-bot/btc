/* BTC Hedge Assistant v8.25.4 - Live Funding Carry Recovery Monitor (read-only) */
(()=>{'use strict';
if(window.__BTC_FUNDING_CARRY_8254__)return;window.__BTC_FUNDING_CARRY_8254__=true;
const V='8.25.4',KEY='btc:funding-carry:v2',LEGACY='btc:funding-carry:v1',SYMBOL='BTCUSDT',REFRESH=5*60*1000;let lastError='',state=null,timer=null;const $=id=>document.getElementById(id),num=v=>Number.isFinite(Number(v))?Number(v):null;
function M(){try{return typeof market!=='undefined'?market:(window.market||{})}catch(e){return window.market||{}}}
function P(){try{return typeof pos==='function'?pos():(typeof window.pos==='function'?window.pos():null)}catch(e){return null}}
function load(){try{const x=JSON.parse(localStorage.getItem(KEY)||'null');if(x)return Object.assign({manualDailyUsd:1,manualFallback:true},x);const old=JSON.parse(localStorage.getItem(LEGACY)||'{}');return{manualDailyUsd:num(old.dailyUsd)??1,manualFallback:true}}catch(e){return{manualDailyUsd:1,manualFallback:true}}}
function savePatch(p){const x=Object.assign(load(),p,{updatedAt:Date.now()});try{localStorage.setItem(KEY,JSON.stringify(x))}catch(e){lastError=String(e?.message||e)}return x}
async function getJSON(url,ms=6500){const c=new AbortController(),t=setTimeout(()=>c.abort(),ms);try{const r=await fetch(url,{cache:'no-store',signal:c.signal});if(!r.ok)throw new Error('HTTP '+r.status);return await r.json()}finally{clearTimeout(t)}}
async function refresh(force=false){const old=load();if(!force&&state&&Date.now()-(state.fetchedAt||0)<REFRESH)return state;try{const [premium,hist]=await Promise.all([getJSON(`https://fapi.binance.com/fapi/v1/premiumIndex?symbol=${SYMBOL}`),getJSON(`https://fapi.binance.com/fapi/v1/fundingRate?symbol=${SYMBOL}&limit=1000`)]);const rows=(Array.isArray(hist)?hist:[]).map(x=>({t:Number(x.fundingTime),r:Number(x.fundingRate)})).filter(x=>Number.isFinite(x.t)&&Number.isFinite(x.r)).sort((a,b)=>a.t-b.t),now=Date.now(),avg=days=>{const a=rows.filter(x=>x.t>=now-days*864e5);return a.length?a.reduce((s,x)=>s+x.r,0)/a.length:null};state={source:'BINANCE_FUTURES',symbol:SYMBOL,currentRate:num(premium?.lastFundingRate),nextFundingTime:num(premium?.nextFundingTime),markPrice:num(premium?.markPrice),avg7d:avg(7),avg30d:avg(30),samples7d:rows.filter(x=>x.t>=now-7*864e5).length,samples30d:rows.filter(x=>x.t>=now-30*864e5).length,fetchedAt:now,stale:false,error:''};savePatch({lastLive:state,manualDailyUsd:old.manualDailyUsd,manualFallback:old.manualFallback});lastError='';render();document.dispatchEvent(new CustomEvent('btc-funding-carry-updated',{detail:calc()}));return state}catch(e){lastError=String(e?.message||e);const cached=old.lastLive||null;state=cached?Object.assign({},cached,{stale:true,error:lastError}):null;render();return state}}
function rateDailyUsd(rate,price,d){if(rate==null||!price||!d)return null;const lq=Math.max(0,num(d.lq)||0),sq=Math.max(0,num(d.sq)||0);/* Positive Binance rate: longs pay, shorts receive. Three 8H settlements/day is the standard estimate; actual interval can change. */return price*rate*3*(sq-lq)}
function calc(){const s=load(),d=P(),mp=num(state?.markPrice)??num(M()?.price),p=num(M()?.price)??mp;let unreal=null;if(d&&p&&num(d.lq)!=null&&num(d.sq)!=null&&num(d.le)!=null&&num(d.se)!=null)unreal=d.lq*(p-d.le)+d.sq*(d.se-p);const loss=unreal!=null?Math.max(0,-unreal):null,liveFresh=!!state&&!state.stale&&Date.now()-(state.fetchedAt||0)<20*60*1000,currentDaily=liveFresh?rateDailyUsd(state.currentRate,mp,d):null,d7=liveFresh?rateDailyUsd(state.avg7d,mp,d):null,d30=liveFresh?rateDailyUsd(state.avg30d,mp,d):null,useDaily=currentDaily!=null?currentDaily:(s.manualFallback?s.manualDailyUsd:null),conservative=[d7,d30].filter(Number.isFinite).sort((a,b)=>a-b)[0]??useDaily,positiveDaily=Math.max(0,conservative||0),days=loss!=null&&positiveDaily>0?loss/positiveDaily:null;return{version:V,readOnly:true,source:currentDaily!=null?'BINANCE_LIVE':'MANUAL_FALLBACK',symbol:SYMBOL,currentRate:state?.currentRate??null,avg7d:state?.avg7d??null,avg30d:state?.avg30d??null,nextFundingTime:state?.nextFundingTime??null,markPrice:mp,longQty:num(d?.lq),shortQty:num(d?.sq),currentDailyUsd:currentDaily,avg7dDailyUsd:d7,avg30dDailyUsd:d30,manualDailyUsd:s.manualDailyUsd,manualFallback:s.manualFallback,usedDailyUsd:useDaily,conservativeDailyUsd:conservative,monthlyUsd:conservative!=null?conservative*30:null,annualUsd:conservative!=null?conservative*365:null,unrealizedUsd:unreal,lossUsd:loss,offsetDays:days,offsetYears:days!=null?days/365:null,fetchedAt:state?.fetchedAt||0,stale:!liveFresh,lastError,warning:'Funding is variable and can reverse. Carry never relaxes Safety/Guard.'}}
function style(){if($('btcFundingCarryStyle'))return;const e=document.createElement('style');e.id='btcFundingCarryStyle';e.textContent=`#btcFundingCarryCard{border:1px solid #3b5066;border-radius:14px;background:#0b1723;padding:12px;margin:10px 0}.fcHead{display:flex;justify-content:space-between;gap:8px;align-items:center}.fcHead b{font-size:14px}.fcBadge{font-size:9px;border:1px solid #49627a;border-radius:999px;padding:3px 6px;color:#a9bbcd}.fcGrid{display:grid;grid-template-columns:repeat(3,1fr);gap:6px;margin-top:8px}.fcBox{border:1px solid #293d51;border-radius:9px;padding:7px}.fcBox span{display:block;font-size:9px;color:#91a2b4}.fcBox strong{font-size:13px}.fcSet{display:flex;gap:6px;align-items:center;flex-wrap:wrap;margin-top:8px;font-size:10px}.fcSet input{width:74px;background:#07121d;color:#fff;border:1px solid #40566d;border-radius:7px;padding:6px}.fcSet button{min-height:32px;border-radius:7px}.fcNote{font-size:9px;line-height:1.5;color:#9aacbd;margin-top:7px}@media(max-width:520px){.fcGrid{grid-template-columns:1fr 1fr}}`;document.head.appendChild(e)}
function fmt(v,d=1){return num(v)!=null?Number(v).toLocaleString(undefined,{maximumFractionDigits:d}):'--'}function pct(v){return num(v)!=null?(Number(v)*100).toFixed(4)+'%':'--'}
function render(){style();const page=$('v8222TodayAnalysisPage');if(!page)return false;let c=$('btcFundingCarryCard');if(!c){c=document.createElement('div');c.id='btcFundingCarryCard';const a=$('btcHedgeActionValidationCard')||$('btcForwardValidationCard');a?.insertAdjacentElement('afterend',c);if(!c.isConnected)page.appendChild(c)}const x=calc(),offset=x.offsetDays==null?(x.lossUsd===0?'현재 상쇄 대상 손실 없음':'산출 대기'):`${fmt(x.offsetDays,0)}일 ≈ ${fmt(x.offsetYears,1)}년`,src=x.source==='BINANCE_LIVE'?'BINANCE LIVE':'MANUAL FALLBACK',next=x.nextFundingTime?new Date(x.nextFundingTime).toLocaleString():'--';c.innerHTML=`<div class="fcHead"><b>펀딩비 · 손실회복 기여도</b><span class="fcBadge">${src} · READ-ONLY</span></div><div class="fcGrid"><div class="fcBox"><span>현재 / 7일 / 30일 Funding</span><strong>${pct(x.currentRate)} / ${pct(x.avg7d)} / ${pct(x.avg30d)}</strong></div><div class="fcBox"><span>순 Carry 일평균 현재 / 7일 / 30일</span><strong>$${fmt(x.currentDailyUsd,2)} / $${fmt(x.avg7dDailyUsd,2)} / $${fmt(x.avg30dDailyUsd,2)}</strong></div><div class="fcBox"><span>보수적 30일 / 1년</span><strong>$${fmt(x.monthlyUsd,0)} / $${fmt(x.annualUsd,0)}</strong></div><div class="fcBox"><span>현재 손실 단순 상쇄기간</span><strong>${offset}</strong></div><div class="fcBox"><span>포지션 롱 / 숏</span><strong>${fmt(x.longQty,2)} / ${fmt(x.shortQty,2)} BTC</strong></div><div class="fcBox"><span>다음 Funding 시각</span><strong>${next}</strong></div></div><div class="fcSet">API 실패시 일평균 $ <input id="btcFundingDailyInput" inputmode="decimal" value="${x.manualDailyUsd}"><button id="btcFundingSave">Fallback 저장</button><button id="btcFundingRefresh">Funding 새로고침</button></div><div class="fcNote">Binance Futures의 현재 Funding Rate와 최근 7일·30일 평균을 자동 조회하고, 롱 지급·숏 수취를 서로 상계한 순 Carry를 추정합니다. 보수적 전망은 7일/30일 추정 중 낮은 값을 사용합니다. API가 실패할 때만 수동값을 fallback으로 사용합니다. Funding은 변동·역전될 수 있고 실제 정산 간격도 변할 수 있으므로 참고치이며, Safety/Guard를 완화하거나 위험한 포지션 유지 근거로 사용하지 않습니다.${x.lastError?' · 최근 API 오류: '+x.lastError:''}</div>`;c.querySelector('#btcFundingSave')?.addEventListener('click',()=>{savePatch({manualDailyUsd:Number(c.querySelector('#btcFundingDailyInput')?.value)||0,manualFallback:true});render()});c.querySelector('#btcFundingRefresh')?.addEventListener('click',()=>refresh(true));return true}
function boot(){state=load().lastLive||null;render();refresh();document.addEventListener('btc-bootstrap-ready',()=>setTimeout(()=>refresh(),900));document.addEventListener('btc-forward-validation-evaluated',()=>setTimeout(render,100));document.addEventListener('visibilitychange',()=>{if(!document.hidden)setTimeout(()=>refresh(),500)});timer=setInterval(()=>{if(!document.hidden)refresh()},REFRESH);window.BTCFundingCarry={version:V,key:KEY,readOnly:true,calc,refresh,saveFallback:v=>savePatch({manualDailyUsd:Number(v)||0,manualFallback:true}),render,status:calc}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();